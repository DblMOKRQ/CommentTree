# Древовидные комментарии (CommentTree)

Простой, но мощный сервис на Go для реализации древовидной (иерархической) системы комментариев. Проект включает в себя RESTful API на бэкенде и минималистичный веб-интерфейс на фронтенде.

В основе хранения иерархии лежит подход **Materialized Path**, который позволяет эффективно выбирать целые ветки комментариев одним запросом к базе данных.


## ✨ Основные возможности

*   **Иерархическая структура:** Создание комментариев верхнего уровня и ответов на любой глубине вложенности.
*   **Полный CRUD:** Создание, чтение и удаление комментариев.
*   **Эффективная выборка:** Получение целого дерева комментариев за минимальное количество запросов к БД.
*   **Пагинация:** Постраничный вывод для комментариев верхнего уровня.
*   **Сортировка:** Возможность сортировки корневых комментариев по дате создания или ID.
*   **Поиск:** Простой полнотекстовый поиск по тексту комментариев (через `ILIKE`).
*   **Готовый веб-интерфейс:** Простой UI на HTML/CSS/JS для демонстрации всех возможностей API.

## 🛠️ Технологический стек

*   **Бэкенд:**
    *   **Go**: Основной язык программирования.
    *   **PostgreSQL**: Реляционная база данных для хранения комментариев.
    *   **wb-go/wbf**: Форк фреймворка Gin для роутинга и обработки HTTP-запросов.
    *   **golang-migrate**: Инструмент для управления миграциями базы данных.
*   **Фронтенд:**
    *   HTML5
    *   CSS3 (без фреймворков)
    *   JavaScript (Vanilla JS, Fetch API)

## 🚀 Установка и запуск

Для запуска проекта локально вам понадобятся Go и PostgreSQL.

**1. Клонируйте репозиторий:**
```bash
git clone <URL вашего репозитория>
cd <название папки>
```

**2. Настройте конфигурацию:**
Скопируйте `config/config.example.yaml` в `config/config.yaml` и измените параметры подключения к вашей базе данных PostgreSQL.

```yaml
# config/config.yaml
addr: ":8080"
log_level: "debug"
master_dsn: "postgres://user:password@localhost:5432/comments_db?sslmode=disable"
slaveDSNs: []
```
*Убедитесь, что база данных `comments_db` создана.*

**3. Установите зависимости:**
```bash
go mod tidy
```

**4. Запустите приложение:**
При первом запуске миграции будут применены автоматически, и в базе данных создастся необходимая таблица `comments`.
```bash
go run main.go
```

**5. Откройте в браузере:**
Перейдите по адресу [http://localhost:8080](http://localhost:8080), чтобы увидеть веб-интерфейс.

## 📖 API Endpoints

Сервис предоставляет следующие эндпоинты:

#### `GET /comments`
Получение списка деревьев комментариев с пагинацией.
*   **Query-параметры:**
    *   `page` (int, опционально, по умолчанию `1`): Номер страницы.
    *   `limit` (int, опционально, по умолчанию `10`): Количество корневых комментариев на странице.
    *   `sort_by` (string, опционально, по умолчанию `created_at`): Поле для сортировки (`created_at` или `id`).
    *   `sort_order` (string, опционально, по умолчанию `asc`): Порядок сортировки (`asc` или `desc`).
*   **Ответ (`200 OK`):**
    ```json
    {
        "comments": [ { /* ...объект комментария с вложенными children... */ } ],
        "total": 15,
        "page": 1,
        "limit": 10
    }
    ```

#### `GET /comments?parent={id}`
Получение одного полного дерева комментариев, начиная с указанного `id`.
*   **Ответ (`200 OK`):**
    ```json
    {
        "comments": { /* ...объект комментария с вложенными children... */ }
    }
    ```

#### `POST /comments`
Создание нового комментария.
*   **Тело запроса (JSON):**
    *   Для корневого комментария: `{ "comment": "Текст моего комментария" }`
    *   Для ответа: `{ "comment": "Текст ответа", "parent_id": 123 }`
*   **Ответ (`201 Created`):**
    ```json
    { "comment": comment }
    ```

#### `DELETE /comments/{id}`
Удаление комментария и всей его ветки (всех дочерних комментариев).
*   **Path-параметр:**
    *   `id` (int): ID удаляемого комментария.
*   **Ответ (`200 OK`):**
    ```json
    { "id": 123 }
    ```

#### `GET /search`
Полнотекстовый поиск по комментариям.
*   **Query-параметры:**
    *   `q` (string, обязательно): Поисковый запрос.
*   **Ответ (`200 OK`):**
    ```json
    {
        "comments": [ { /* ...плоский список найденных комментариев... */ } ]
    }
    ```

## 🗄️ Структура Базы Данных

Проект использует одну таблицу `comments` со следующей структурой:

| Поле         | Тип                      | Описание                                                                                                |
|--------------|--------------------------|---------------------------------------------------------------------------------------------------------|
| `id`         | `BIGSERIAL PRIMARY KEY`  | Уникальный идентификатор комментария.                                                                   |
| `parent_id`  | `BIGINT`                 | ID родительского комментария. `NULL` для комментариев верхнего уровня.                                  |
| `path_id`    | `UUID`                   | Уникальный UUID, генерируемый приложением. Используется для построения пути.                            |
| `path`       | `TEXT`                   | **Materialized Path**. Путь от корня до текущего комментария, составленный из `path_id`. Пример: `uuid1/uuid2/`. |
| `comment`    | `TEXT`                   | Текст комментария.                                                                                      |
| `created_at` | `TIMESTAMP WITH TIME ZONE` | Дата и время создания.                                                                                |

